[project]
name = "we-remember"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
    "django==5.2.*",
]

[dependency-groups]
dev = [
    "ruff>=0.15.13",
]

[tool.ruff]
line-length = 88
target-version = "py313"
